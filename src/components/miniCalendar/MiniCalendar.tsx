import React, { ReactElement } from 'react'
import styles from './MiniCalendar.module.scss'

export const MiniCalendar = (): ReactElement => {
  return <div className={styles.miniCalendar}>Mini Calendar</div>
}
